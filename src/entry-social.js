// Entry point for the Social Norms experiment (social.html → /social).
// The mode is fixed here — the participant never sees a mode selector.
import { boot } from './main.js';

boot('SOCIAL_NORMS');
